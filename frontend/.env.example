# Base URL of the backend API.
# Leave empty for local dev (Vite proxies /api to http://localhost:8082 — see vite.config.ts)
# and for production, where nginx serves the frontend and proxies /api on the same origin.
VITE_API_URL=
